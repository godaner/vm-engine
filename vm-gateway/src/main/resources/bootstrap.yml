# spring cloud config
spring:
  application:
    name: vm-gateway
  cloud:
    config:
      label: dev #branch
      profile: @profileActive@
      discovery:
        enabled: true
        serviceId: vm-config-server
#spring.cloud.config.uri= http://localhost:8888/
