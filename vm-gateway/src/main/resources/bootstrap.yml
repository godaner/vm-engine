# spring cloud config
spring:
  application:
    name: vm-gateway
  cloud:
    config:
      label: dev
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config
#spring.cloud.config.uri= http://localhost:8888/
