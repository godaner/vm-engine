# spring cloud config
spring:
  application:
    name: vm-provider-admin
  cloud:
    config:
      label: dev
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config
