# spring cloud config
spring:
  application:
    name: vm-provider-admin
  cloud:
    config:
      label: dev #branch
      profile: @profileActive@
      discovery:
        enabled: true
        serviceId: vm-config-server
