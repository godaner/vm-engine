<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vm.admin.dao.mapper.custom.CustomVmAdminsRolesRealationMapper">
    <select id="getRoleIdsByAdminId" resultType="long">
        SELECT
        DISTINCT role_id
        from vm_admins_roles_realation
        WHERE
        is_deleted = #{query.isDeleted}
        <if test="query.status!=null">
            and status = #{query.status}
        </if>
        and admin_id =#{query.adminId}
    </select>
    <select id="getAdminIdsByRoleId" resultType="long">
        SELECT
        DISTINCT admin_id
        from vm_admins_roles_realation
        WHERE
        is_deleted = #{query.isDeleted}
        <if test="query.status!=null">
            and status = #{query.status}
        </if>
        and role_id =#{query.roleId}
    </select>

</mapper>