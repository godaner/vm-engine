# spring cloud config
spring:
  application:
    name: vm-provider-user
  cloud:
    config:
      label: master #branch
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config-server
