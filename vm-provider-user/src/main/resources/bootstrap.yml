# spring cloud config
spring:
  application:
    name: vm-provider-user
  cloud:
    config:
      label: dev #branch
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config-server
