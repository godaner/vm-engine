# spring cloud config
spring:
  application:
    name: vm-provider-movie
  cloud:
    config:
      label: dev
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config
