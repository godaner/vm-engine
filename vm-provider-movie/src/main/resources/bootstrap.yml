# spring cloud config
spring:
  application:
    name: vm-provider-user
  cloud:
    config:
      label: zk_spring_cloud
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config
