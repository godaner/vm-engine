# spring cloud config
spring:
  application:
    name: vm-provider-movie
  cloud:
    config:
      label: zk_spring_cloud
      profile: dev
      discovery:
        enabled: true
        serviceId: vm-config

#spring.cloud.config.uri= http://localhost:8888/
eureka:
  client:
    serviceUrl:
      defaultZone: http://server01:1111/eureka/,http://server02:1112/eureka/
---
spring:
  profiles: providerMovie01
server:
  port: 3331
---
spring:
  profiles: providerMovie02
server:
  port: 3332
