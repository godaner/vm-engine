<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vm.movie.dao.mapper.VmMoviesTagsRealationMapper">
<resultMap type="com.vm.movie.dao.po.VmMoviesTagsRealation"  id="VmMoviesTagsRealationMap">
  <id column="id" property="id"/>
  <result column="movie_id" property="movieId"/>
  <result column="tag_id" property="tagId"/>
  <result column="status" property="status"/>
  <result column="create_time" property="createTime"/>
  <result column="update_time" property="updateTime"/>
</resultMap>

    <sql id="BASE_ALL_CLOUM">
        id,
        <include refid="BASE_COLUM_WITHOUT_ID"/>
    </sql>

	<sql id="BASE_COLUM_WITHOUT_ID">
		movie_id,
		tag_id,
		status,
		create_time,
		update_time
	</sql>



	<select id="count" resultType="int">
		SELECT 
			count(1)
		FROM
		vm_movies_tags_realation
		<include refid="queryCondition" />
	</select>

<insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO vm_movies_tags_realation(
        <include refid="BASE_COLUM_WITHOUT_ID"/>
        )
        VALUES (
#{movieId},
#{tagId},
#{status},
#{createTime},
#{updateTime}
       )
				 </insert>

<delete id="delete">
		DELETE
		FROM
			vm_movies_tags_realation
		WHERE
			id = #{id}
	</delete>

<delete id="deleteBy">
		DELETE
		FROM
			vm_movies_tags_realation
		<include refid="queryCondition" />
	</delete>

<update id="update" >
UPDATE
		vm_movies_tags_realation
<include refid="setFields" />
WHERE
id=#{id}
</update>


<update id="batchUpdate">
UPDATE
		vm_movies_tags_realation
<include refid="setFields" />
<include refid="queryCondition" />
</update>


	<select id="selectList" resultMap="VmMoviesTagsRealationMap">
		SELECT
			<include refid="BASE_ALL_CLOUM"/>
		FROM
		vm_movies_tags_realation
		<include refid="queryCondition" />
		LIMIT #{start},#{size}

	</select>

	<select id="selectListOrderBy" resultMap="VmMoviesTagsRealationMap">
		SELECT
			<include refid="BASE_ALL_CLOUM"/>
		FROM
		vm_movies_tags_realation
		<include refid="queryCondition" />
	<if test="orderBy != null and orderType != null">
			order by ${orderBy} ${orderType}
		</if>
		limit #{start},#{size}

	</select>

<sql id="queryCondition">
<trim prefix="where" prefixOverrides="and">
<if test="query != null">
<if test="query.movieId != null ">
		and movie_id=#{query.movieId}
</if>
<if test="query.tagId != null ">
		and tag_id=#{query.tagId}
</if>
<if test="query.status != null ">
		and status=#{query.status}
</if>
<if test="query.createTime != null ">
		and create_time=#{query.createTime}
</if>
<if test="query.updateTime != null ">
		and update_time=#{query.updateTime}
</if>
</if>
</trim>
</sql>


<sql id="setFields">
<set>
<trim suffix="" suffixOverrides=",">
<if test="newObj.movieId != null">movie_id=#{newObj.movieId},</if>
<if test="newObj.tagId != null">tag_id=#{newObj.tagId},</if>
<if test="newObj.status != null">status=#{newObj.status},</if>
<if test="newObj.createTime != null">create_time=#{newObj.createTime},</if>
<if test="newObj.updateTime != null">update_time=#{newObj.updateTime}</if>
</trim>
</set>
</sql>


<insert id="batchInsert"  useGeneratedKeys="true" parameterType="java.util.List" keyProperty="id">
        INSERT INTO vm_movies_tags_realation(
        <include refid="BASE_COLUM_WITHOUT_ID"/>
)        VALUES 
   <foreach collection ="list" item ="item" index ="index" separator =","> 
(
#{item.movieId},
#{item.tagId},
#{item.status},
#{item.createTime},
#{item.updateTime}
)
</foreach> 
</insert>


    <select id="select" resultMap="VmMoviesTagsRealationMap">
        SELECT * FROM vm_movies_tags_realation WHERE id = #{id}
    </select>


    <select id="selectInIds" resultMap="VmMoviesTagsRealationMap">
        SELECT
        <include refid="BASE_ALL_CLOUM"/>
        FROM
        vm_movies_tags_realation
        WHERE id IN
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>


    <select id="selectIdList" resultType="java.lang.Long">
        SELECT
 		 id 
        FROM
        vm_movies_tags_realation
        <include refid="queryCondition" />

    </select>


	<select id="selectBy" resultMap="VmMoviesTagsRealationMap">
		SELECT * 
		FROM
		vm_movies_tags_realation
		<include refid="queryCondition" />
	</select>


	<select id="selectOneBy" resultMap="VmMoviesTagsRealationMap">
		SELECT * 
		FROM
		vm_movies_tags_realation
		<include refid="queryCondition" />
		LIMIT 1 
	</select>

</mapper>